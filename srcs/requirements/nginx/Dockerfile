# debian penultimate stable version
FROM debian:bullseye

# update and upgrade apt, install NGINX, Vim, Curl & OpenSSL
RUN apt-get update && apt-get upgrade -y && apt-get install -y nginx vim curl openssl php-fpm



RUN



COPY




CMD [   ]
